{
  "name": "ai-visibility-reporting",
  "version": "0.3.0",
  "private": true,
  "description": "AI Visibility Reporting Tool — Phase 1 MVP. Runs the monthly 13-platform audit, scores it, writes to Google Sheets, archives raw JSON, and delivers a PDF report to Google Drive. Triggered by the VA from index.html → Cloudflare Worker /trigger-report → GitHub Actions → runner.js.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node runner.js"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
