AI VISIBILITY REPORTING TOOL — MASTER REFERENCE DOC
Version: V1.3 (working copy)
Last Updated: 2026-05-14
Governing Spec: AI_Visibility_Reporting_Tool_Master_Spec_V1.0.txt
Build Plan: Docs/AI_Visibility_Tool_Master_Build_Plan_V1_1.txt
Architectural Model: AEO Article Creation Tool (proven pattern — extend, don't reinvent)
================================================

CURRENT STATUS — PHASE 1 / SESSION 5 COMPLETE — FEATURE-COMPLETE
---------------------------------------------------------------
Phase 1 (MVP — VA-triggered, single client per run) is now FEATURE-COMPLETE
in code. The pipeline runs end-to-end from --client argument to a PDF
uploaded to the client's Drive "Monthly Reports" folder. Remaining work is
operational, not coded:
  - Cloudflare Worker /trigger-report endpoint (manual route addition;
    see "MANUAL SETUP" section below).
  - GitHub Secrets provisioning (6 secrets).
  - First end-to-end test run against the iTech Valet sheet.

Five sessions have shipped:

  Session 1 — repo scaffolding + iTech Valet client config + 13 prompt
              templates with runtime variable injection.  COMPLETE.

  Session 2 — runner.config.js (platform endpoints, sonar model locked,
              30s→60s→120s→240s→480s backoff schedule), runner.js
              (full pipeline orchestrator, serial 13-run loop, env-only
              API keys, graceful retry, never-crash policy), parser.js
              (Claude Haiku via the existing Cloudflare Worker proxy +
              heuristic regex fallback), prompts/parser-prompt.js
              (strict-JSON system prompt + 3 few-shot examples).
              Build plan corrected: every "12 runs" / "Twelve runs" /
              "12 API calls" / "12 rows" / "12 platform runners"
              reference is now "13" (Run 6 is intentional and stays —
              it is INTERNAL ONLY, never client-facing).  COMPLETE.

  Session 3 — Session 2 fixes applied (Run 3 now injects authority_figure
              + authority_title into the platform prompt; Perplexity's
              top-level citations array is now captured by the runner
              and forwarded to parser.parseResponse where it overrides
              cited_urls when present). Built: score-engine.js (pure JS
              composite score with insufficient_data handling, trend +
              benchmark), sheet-reader.js + sheet-writer.js (googleapis
              service-account auth from GOOGLE_SERVICE_ACCOUNT_JSON,
              tabs targeted by NAME, missing-tab errors with master-
              template guidance, duplicate-month protection), github-
              archiver.js (local fs JSON backup; the Actions workflow
              handles the git commit + push). runner.js wired through
              step 5 with real calls + signatures; steps 6–8 (narrative,
              PDF, Drive upload) remain typeof-guarded scaffold calls.
              package.json added declaring googleapis ^144.0.0.  COMPLETE.

  Session 4 — Cloudflare Worker 400 fix applied (parser.js now sends
              x-api-key from process.env.ANTHROPIC_API_KEY — the Worker
              is a pass-through, not an injecting proxy). Built: the
              full prompts/narrative-prompt.js (8 locked sections,
              voice + data rules from 4.3A, 19-word forbidden list,
              ALIGNMENT SIGNAL detection, Month 1 NAP-clean win priority,
              section-by-section instructions adapted to clientMonth /
              progressionPhase / isFirstMonth / isQuarterlyMonth / score
              null state), narrative-engine.js (direct call to
              api.anthropic.com/v1/messages with model claude-sonnet-
              4-20250514, max_tokens 4000, temperature 0.7, 90s timeout,
              returns null on any failure), pdf-builder.js (Puppeteer
              headless Chromium → Letter PDF; splits Claude's narrative
              on the 8 literal section headers; escapes + paragraph-
              wraps each block; populates templates/report.html;
              outputs to outputs/<client_id>_AI_Visibility_<YYYY-MM>.pdf;
              fails to null on any error), templates/report.html
              (Brighton Gold aesthetic — Helvetica display, Georgia
              body, 1in margins, page-break-before on every section
              except OPENING, WIN callout as a grey-block aside with
              border-left, page numbers via Puppeteer footerTemplate,
              zero external resources). sheet-reader.js extended with
              readAEOInventory (resolves columns by NAME — STATUS,
              CLUSTER_NUMBER, CLUSTER_NAME, STRATEGIC_OBJECTIVE — and
              returns { totalLiveNodes, currentCluster, nextCluster };
              STRATEGIC_OBJECTIVE is optional, nulls handled). runner.js
              now reads AEO_Inventory, fetches 4.3A + 4.3B from Google
              Drive (Docs export to text/plain, plain files via
              files.get?alt=media; placeholder/empty IDs short-circuit
              to ""), computes progressionPhase / isFirstMonth /
              isQuarterlyMonth, assembles the full narrative context,
              and wires steps 6 (generateNarrative) + 7 (buildPDF) to
              real calls. package.json bumped — puppeteer ^23.0.0 added.
              Only drive-writer.js, index.html, and the GitHub Actions
              workflow remain.  COMPLETE.

  Session 5 — Built the two-call architecture: analysis-engine.js
              runs FIRST (Claude Sonnet @ temp 0.3) and produces a
              structured Strategic Brief; narrative-engine.js then
              reads that brief and writes the client-facing prose
              (Sonnet @ temp 0.7). Analysis sees ALL 13 runs (including
              Run 6); narrative only sees the 12 client-eligible runs.
              Built citation-history tracking in sheet-reader.js
              (readCitationHistory groups cited_urls across all prior
              months and returns URLs that appear in 2+ distinct
              months — a "citation echo" is the strongest leading
              indicator of AI authority). Built drive-writer.js
              (Drive v3 upload with update-if-exists, placeholder-id
              skip, no crash on any failure). Built the VA-facing
              index.html (client dropdown, single Run Report button,
              POSTs to the Cloudflare Worker /trigger-report endpoint).
              Built .github/workflows/run-report.yml (workflow_dispatch
              + repository_dispatch entry points, Puppeteer apt deps,
              archive auto-commit). Updated narrative-prompt.js to
              consume the Strategic Brief + repeatedCitations.
              Rewired runner.js to the 13-step architecture (config →
              Drive docs → sheet reads → 13 runs → score → sheet write
              → archive → analysis → narrative → PDF → Drive upload →
              completion log). package.json bumped to 0.5.0 with
              @anthropic-ai/sdk added per spec. The Cloudflare Worker
              /trigger-report endpoint is documented as a manual setup
              step — not auto-built.   COMPLETE.

Sessions 6+ are operational:
  - Add the /trigger-report route to the existing Cloudflare Worker
    (validates x-internal-key, fires GitHub repository_dispatch).
    See "MANUAL SETUP" below.
  - Provision the 6 GitHub Secrets (ANTHROPIC_API_KEY, OPENAI_API_KEY,
    GEMINI_API_KEY, PERPLEXITY_API_KEY, GROK_API_KEY,
    GOOGLE_SERVICE_ACCOUNT_JSON).
  - Replace INTERNAL_ACCESS_KEY in index.html with the real value
    paired to the Worker.
  - First test run against the iTech Valet sheet (set its
    sheet_id + drive_reports_folder_id + doc_43A_id + doc_43B_id in
    clients/itechvalet.json before triggering).

================================================

THE ONE THING THAT MATTERS
--------------------------
This tool is a client retention system disguised as reporting. It proves
the program is working — especially in months 1–6 when churn risk is
highest. Every architectural decision serves that goal.

Architectural law (inherited from the AEO tool):
  Code executes on fixed specs; Claude writes narrative only.
  Every generative responsibility moved from interpretation to
  execution improves reliability.

================================================

ARCHITECTURE — PIPELINE FLOW
----------------------------

CLIENT CONFIG (JSON, one per client)
        ↓
VA TRIGGERS RUN (index.html → Cloudflare Worker dispatch → GitHub Action)
        ↓
RUNNER (Node.js in GitHub Actions — orchestrates everything)
        ↓
GOOGLE DRIVE FETCH (4.3A + 4.3B — same pattern as AEO tool)
        ↓
SHEET READER (reads full AI_Visibility_Raw + Summary history)
        ↓
13 API CALLS — serial, one at a time (ChatGPT, Gemini, Perplexity/sonar, Grok)
        ↓
CLAUDE HAIKU PARSER per run (strict JSON schema + heuristic fallback)
        ↓
SCORE ENGINE (JS calculates AI Visibility Score — never Claude)
        ↓
GOOGLE SHEET WRITER (AI_Visibility_Raw + AI_Visibility_Summary tabs)
        ↓
GITHUB ARCHIVER (raw JSON backup — [YYYY-MM]/[client_id]_raw.json)
        ↓
CLAUDE SONNET NARRATIVE ENGINE (report written in Gerek's voice)
        ↓
PUPPETEER PDF BUILDER (named [client_id]_AI_Visibility_[YYYY-MM].pdf)
        ↓
GOOGLE DRIVE WRITER (PDF → client's "Monthly Reports" folder)
        ↓
[PHASE 3] EMAIL DELIVERY (PDF link, simple template)
        ↓
LOOKER STUDIO DASHBOARD (manual setup — reads Sheet automatically)

Key laws:
  - AI Visibility Score is calculated by JS. Claude NEVER calculates or
    interprets the score.
  - Claude receives the score, trend, and benchmark as pre-calculated facts.
  - Dates are never calculated by Claude. run_date, report_period, and
    client_month are passed as facts.
  - API calls are SERIAL. Never parallel. Clean error isolation per run.
  - Run 6 is INTERNAL ONLY. Never reaches the narrative engine. Sheet-only.

================================================

REPO & HOSTING
--------------
Repo:      github.com/iTechValet/iTV-Tools
Subfolder: AI-Visibility-Reporting/
Hosting:   itechvalet.github.io/iTV-Tools/AI-Visibility-Reporting/   (Phase 1)
Cloudflare proxy (reused from AEO):
           anthropic-proxy.gerek.workers.dev
             - Used by parser.js for Claude Haiku
             - Will be used by narrative-engine.js for Claude Sonnet
             - Will gain a /trigger-report dispatch endpoint in a later
               Phase 1 session (Worker fires repository_dispatch to
               GitHub Actions; the browser never sees an API key)

Subfolder layout (as of Session 5):

  iTV-Tools/
    .github/
      workflows/
        run-report.yml         # GitHub Actions workflow — workflow_dispatch
                               #   + repository_dispatch (BUILT — Session 5)
    AI-Visibility-Reporting/
      package.json             # @anthropic-ai/sdk ^0.40 + googleapis ^144
                               #   + puppeteer ^23 (Sessions 3-5)
      index.html               # VA-facing UI (BUILT — Session 5)
      runner.js                # Orchestrator end-to-end, 13 steps,
                               #   Drive upload included (Sessions 2-5)
      runner.config.js         # Platform configs (BUILT — Session 2)
      parser.js                # Haiku + heuristic + citations override
                               #   + x-api-key Worker fix (Sessions 2-4)
      score-engine.js          # Score math (BUILT — Session 3)
      sheet-reader.js          # Summary history + AEO inventory +
                               #   citation history (Sessions 3-5)
      sheet-writer.js          # Raw + Summary writer (BUILT — Session 3)
      github-archiver.js       # Raw JSON archive (BUILT — Session 3)
      analysis-engine.js       # Strategic Brief — Sonnet @ temp 0.3
                               #   (BUILT — Session 5)
      narrative-engine.js      # Claude Sonnet narrative call @ temp 0.7
                               #   (BUILT — Session 4)
      pdf-builder.js           # Puppeteer renderer (BUILT — Session 4)
      drive-writer.js          # PDF upload to client Drive folder
                               #   (BUILT — Session 5)
      prompts/
        prompt-templates.js    # 13 run-prompt templates (Sessions 1 + 3)
        parser-prompt.js       # Haiku system + few-shot (BUILT — Session 2)
        narrative-prompt.js    # Sonnet system + section instructions +
                               #   Strategic Brief + citation context
                               #   (Sessions 4 + 5)
      templates/
        report.html            # PDF template — Brighton Gold design
                               #   (BUILT — Session 4)
      clients/
        itechvalet.json        # Test client config (BUILT — Session 1)
      archive/
        .gitkeep               # Placeholder until first run writes archive
      outputs/                 # Created by pdf-builder on first run;
                               # holds <client_id>_AI_Visibility_<YYYY-MM>.pdf

Every JS/HTML shell carries a top header block stating what the file does,
what calls it, and what it calls.

================================================

WHAT HAS BEEN BUILT — DETAIL
----------------------------

[ Session 1 — May 2026 ]

  clients/itechvalet.json
    First test-client config. iTech Valet itself. Every live ID
    (sheet_id, drive_reports_folder_id, looker_dashboard_id,
    client_email, doc_43A_id, doc_43B_id) is PLACEHOLDER_* per the
    locked-decisions rule against inventing values. business_name,
    service, primary_topic, market_scope, top_competitor,
    authority_figure (Gerek Allen / Founder), customer_term, and
    contract_start (2026-01-01) are filled in for real test use.

  prompts/prompt-templates.js
    All 13 run-ID templates (1A, 1B, 1C, 1D, 2, 3, 4A, 4B, 5A, 5B,
    6, 7A, 7B). Single exported function getPrompt(runId, clientConfig).
    Zero hardcoded client data — every value comes from clientConfig at
    runtime. Required-vars guard rejects empty/missing config keys.
    Unknown-runId throws explicitly. Run 6 flagged INTERNAL ONLY in
    a code comment so the orchestrator can strip it before passing
    runs to Claude Sonnet for the narrative.

  Scaffolds (header comment + STATUS:Scaffold only):
    runner.js, runner.config.js, parser.js, score-engine.js,
    sheet-reader.js, sheet-writer.js, github-archiver.js,
    narrative-engine.js, pdf-builder.js, drive-writer.js,
    prompts/parser-prompt.js, prompts/narrative-prompt.js,
    templates/report.html, index.html.

[ Session 2 — May 2026 ]

  runner.config.js
    Platform endpoint + retry config exported as a plain CommonJS
    module. Four platforms keyed by id:
      openai     → gpt-4o
      gemini     → gemini-1.5-pro
      perplexity → sonar      (sonar specifically — non-negotiable)
      grok       → grok-3
    Each platform entry holds { model, endpoint, secretKey }. The
    secretKey field is the NAME of the env var holding the actual
    key. Keys are NEVER stored in this file (and never in the client
    JSON). retry.maxAttempts = 5; retry.backoff = [30000, 60000,
    120000, 240000, 480000] (ms). timeoutMs = 30000 (per request).

  runner.js
    Pipeline orchestrator. Entry point for GitHub Actions.

    Args:  --client=<client_id>   (also accepts --client <client_id>)

    Startup:
      - Loads clients/<client_id>.json.
      - Computes run_date (today, ISO), report_period (YYYY-MM), and
        client_month (months since contract_start, inclusive).
      - Runs an API-key preflight against process.env and builds a Set
        of available platforms. Missing keys log a clear message and
        the affected runs are recorded with platform_available:false
        and parser_status:"n/a" — the orchestrator never crashes on
        missing keys.

    Pipeline (every step is logged with an ISO timestamp; every step's
    failure is caught + logged + the pipeline continues):
      Step 1: Sheet reader  → prior AI_Visibility_Summary history
                              + prior month score.
      Step 2: 13 platform runs, SERIAL. For each run:
                - getPrompt(runId, clientConfig)
                - callWithRetry(platform, prompt) — 5 attempts with
                  exponential backoff (30s → 60s → 120s → 240s → 480s)
                - On full failure: placeholder row, platform_available
                  set to false, parser_status "n/a", continue.
                - On success: parser.parseResponse(rawResponse, cfg)
                  → row pushed onto results.
              After the loop: count heuristic runs; flip
              heuristic_warning when count >= 4.
      Step 3: score-engine.calculateScore  → composite + trend.
              Summary fields (heuristic_fallback_count,
              heuristic_warning, run_date, report_period,
              client_month) are stamped on by the orchestrator before
              the sheet write — they don't depend on the score engine.
      Step 4: sheet-writer.writeResults  → AI_Visibility_Raw (13 rows)
                                           + AI_Visibility_Summary (1 row).
      Step 5: github-archiver.archive    → /archive/<YYYY-MM>/
                                            <client_id>_raw.json.
      Step 6: narrative-engine.generateNarrative — Run 6 stripped from
              the dataset here (filter r => r.run_id !== '6'), so it
              never reaches Claude.
      Step 7: pdf-builder.buildPdf       → Puppeteer renders
                                            templates/report.html.
      Step 8: drive-writer.uploadPdf     → PDF to client Drive folder.

    Platform call shape:
      - Gemini:    POST endpoint + ?key=<API_KEY>;
                   body { contents:[{ parts:[{ text:prompt }] }] };
                   reads candidates[0].content.parts[0].text.
      - OpenAI / Perplexity (sonar) / Grok (OpenAI-compatible):
                   POST with Bearer auth;
                   body { model, messages:[{ role:'user', content:prompt }] };
                   reads choices[0].message.content.

    Timeout: AbortController-driven, set per request from
             runner.config.timeoutMs (30s).

    Exports (in addition to main): RUN_IDS, RUN_TO_PLATFORM,
            RUN_TO_SIGNAL, parseArgs, calcClientMonth, reportPeriod,
            todayISO — for unit testing.

  parser.js
    Two-tier parser. Single public function:
      async function parseResponse(rawResponse, clientConfig)
        → { brand_mentioned, brand_recommended, brand_position,
            competitor_mentioned, competitor_recommended,
            sentiment_signal, cited_urls, authority_figure_named,
            parser_status }
        where parser_status is "llm" | "heuristic".

    Primary path:
      - POST to https://anthropic-proxy.gerek.workers.dev/v1/messages
        with model "claude-haiku-4-5-20251001", max_tokens 1024,
        anthropic-version: 2023-06-01, body assembled by
        prompts/parser-prompt.js.buildMessages.
      - AbortController timeout (30s).
      - Output recovery: strip stray ```json fences; if direct parse
        fails, slice from first '{' to last '}' and retry.
      - normalizeFields enforces the schema: strict booleans
        (=== true), enum lists for brand_position and sentiment_signal,
        cited_urls coerced to string[].

    Fallback path (heuristicParse):
      - brand_mentioned: case-insensitive substring of business_name.
      - brand_recommended: any of [recommend, call, contact, choose,
        trust, best] within ±80 chars of a brand mention.
      - sentiment_signal: positive vs negative keyword counts →
        positive / negative / mixed / neutral.
      - Every other field defaults to false / "not_mentioned" / [].
      - parser_status set to "heuristic".
    Fires on any error in the primary path — network, timeout,
    non-2xx, empty content, malformed JSON, or invalid schema.

  prompts/parser-prompt.js
    Owns the Haiku payload. SYSTEM_PROMPT enforces strict-JSON output,
    schema shape, and enum lists. buildMessages(rawResponse, cfg)
    returns { system, messages } where messages contains three
    few-shot user/assistant pairs (positive / negative / mixed)
    followed by the live user prompt. The live user prompt matches the
    exact structure in the Session 2 build prompt (business name,
    competitor, response, then the schema body).
    Also exported: SYSTEM_PROMPT, ENUMS, buildUserPrompt.

[ Session 3 — May 2026 ]

  Fix 1 — prompts/prompt-templates.js (Run 3)
    Run 3 previously asked "Who is the recognized expert or authority
    figure behind {business_name}?" — an open discovery question that
    most platforms either guessed wrong on or refused, so the
    authority_figure_named signal was useless. Run 3 now reads:
      "Who is {authority_figure}, the {authority_title} of
       {business_name}? They run a {service} {market_scope}. What is
       this person's personal reputation and professional standing for
       {primary_topic}?"
    REQUIRED_VARS expanded to include authority_figure + authority_title
    so the empty-string guard catches misconfigured clients. The full
    clientConfig is already passed to getPrompt() from runner.js, so no
    runner-side change was needed for Fix 1.

  Fix 2 — parser.js + runner.js (Perplexity citations)
    Perplexity (sonar) returns its sources as a top-level citations
    array on the API response, separate from choices[].message.content.
    Previously the parser only saw the prose, so URLs the platform
    cited were dropped. Now:
      - runner.callPlatform returns { text, citations } for every
        platform — citations is an empty array for non-Perplexity calls
      - The orchestrator destructures { text, citations } in the main
        loop and forwards both to parser.parseResponse(text, cfg, citations)
      - parser.parseResponse accepts an optional 3rd argument and, when
        non-empty, REPLACES cited_urls with the platform's authoritative
        citations on both the Haiku-success and heuristic-fallback paths
      - Defensive parsing: top-level citations OR choices[0].citations,
        filtered for non-empty strings only

  score-engine.js — calculateScore(runs, priorScore) — pure JS, no
    network, no Claude. Filters runs to those where platform_available
    is true. Calculates mention_rate, recommendation_rate,
    sentiment_score as percentages of the available set. Composite =
    40/35/25 weighted, rounded to 1 decimal. If platforms_available
    is 0, returns ai_visibility_score:null + insufficient_data:true
    (never divides by zero). Trend: baseline when priorScore is null
    OR when current score is null; otherwise UP / DOWN / FLAT with
    score_delta rounded to 1 decimal. Benchmark tiers by client_month
    (Months 1–3 / 4–6 / 7+) pulled from the first row that has
    client_month stamped (every row does, including placeholder rows).
    Also returns heuristic_fallback_count and prior_score for the
    sheet write.

  sheet-reader.js — readSheetHistory(clientConfig) — googleapis
    service-account auth from process.env.GOOGLE_SERVICE_ACCOUNT_JSON.
    Targets the AI_Visibility_Summary tab by NAME. Reads the header
    row + every data row below it; maps each row to a HEADER_KEYS-
    indexed object; returns priorScore = last row's
    ai_visibility_score (parsed as Number, null if blank/non-numeric)
    and summaryHistory = the full array. Every failure path
    (missing env var, auth error, missing tab, empty tab, values.get
    failure) returns { priorScore: null, summaryHistory: [] } plus a
    logged error — never crashes the pipeline.

  sheet-writer.js — writeToSheet(clientConfig, runs, scoreResult,
    reportPeriod, runDate, clientMonth) — same auth pattern as the
    reader. Confirms BOTH AI_Visibility_Raw and AI_Visibility_Summary
    tabs exist by NAME; missing tab throws:
      "Tab \"<name>\" not found in sheet <sheet_id>. Ensure this sheet
       was created from the master template."
    Duplicate-month protection: reads the Raw tab and looks for any
    existing row where client_id (col B) AND report_period (col F)
    match this run. If found, BOTH writes are skipped (Raw and
    Summary) and a warning is logged — never overwrites a month.
    Row formatting (per build plan): booleans serialized as TRUE/FALSE
    (Sheets renders them as native booleans via valueInputOption=
    USER_ENTERED); cited_urls comma-joined; raw_response truncated to
    50,000 chars; heuristic_warning auto-derived from
    heuristic_fallback_count >= 4. Run 6 IS written to Raw (internal
    data only; it is stripped at the narrative engine, not at the
    Sheet boundary). Returns { skipped, reason, rawAppended,
    summaryAppended }.

  github-archiver.js — archiveRun(clientId, reportPeriod, runs,
    scoreResult) — writes archive/<YYYY-MM>/<client_id>_raw.json to
    the local filesystem. YYYY-MM is derived from reportPeriod
    (slice(0,7)) — never from new Date() — so a run that crosses UTC
    midnight stays in the right month. Recursive mkdir handles a
    first-month run. File contents: { client_id, report_period,
    archived_at, score_summary, runs }. Pretty-printed (JSON.stringify
    indent=2). Failure modes log + return { written:false, reason }
    — the pipeline never crashes over a backup failure. The GitHub
    Actions workflow handles the eventual git commit + push of any
    new archive files.

  package.json — declares the googleapis ^144.0.0 dependency required
    by sheet-reader.js + sheet-writer.js, plus node engine constraint
    (>=18, needed for built-in fetch + AbortController), plus an
    "npm start" alias for "node runner.js". The GitHub Actions
    workflow (Session 4+) will run npm ci before runner.js.

  runner.js (rewired)
    Step 1 calls sheet-reader.readSheetHistory(clientConfig); destructures
    { priorScore, summaryHistory } and logs both. Platform calls now
    capture { text, citations } and forward citations to
    parser.parseResponse(text, cfg, citations). Step 3 calls
    score-engine.calculateScore(rows, priorScore) and logs score, trend,
    delta, platforms_available, heuristic count. Step 4 calls
    sheet-writer.writeToSheet(clientConfig, rows, scoreResult, period,
    runDate, clientMonth) and logs append counts (or skipped reason).
    Step 5 calls github-archiver.archiveRun(clientId, period, rows,
    scoreResult) and logs the written path + byte count. Step 6 still
    invokes narrative-engine.generateNarrative as a typeof-guarded
    call but now passes scoreResult and summaryHistory (instead of the
    Session 2 partial summary object). Steps 7–8 unchanged.

[ Session 4 — May 2026 ]

  Fix — parser.js Cloudflare Worker 400 error
    Session 3 smoke testing surfaced HTTP 400 "Missing x-api-key header"
    from the existing anthropic-proxy worker. The Worker is a
    pass-through, NOT an injecting proxy. parser.js now reads
    process.env.ANTHROPIC_API_KEY at call time and sends it as the
    x-api-key header. Missing-key path throws and the heuristic fallback
    fires. Verified by mock: with the key set, parser stays on the LLM
    path; with no key, it falls back to heuristic and the pipeline
    still runs.

  prompts/narrative-prompt.js — buildNarrativePrompt(context)
    Returns { system, messages } shaped for the Anthropic Messages API.
    System prompt: voice/identity rules pulled from 4.3A (with a sane
    fallback when 4.3A is empty), 19-word forbidden list (deployed,
    clusters, intent layers, pillars, semantic, entity, trunk, branch,
    schema, backlink, keyword, SEO, Tier 1/2/3, synergy, leverage,
    optimize, utilize), competitor name suppressed ("the top name in
    your market"), data rules ("never compute scores", "every number
    framed with context", score-with-benchmark shape), forbidden-phrase
    list ("just wait", "as expected" without explanation, Drift
    Detection), and structural rules locking the 8 section names as
    literal ALL-CAPS headers with no markdown.
    User prompt: pre-calculated facts (run_date, reportPeriod label,
    clientMonth, progressionPhase, flags, full scoreResult, full
    summaryHistory, AEO inventory, 4.3B identity, 12 narrative-eligible
    runs each with raw_response truncated to 2000 chars and full parsed
    fields) followed by section-by-section instructions that adapt to:
      - Month 1 vs Month 2-5 vs Month 6+ in OPENING
      - isQuarterlyMonth (adds the AI Authority Quarterly Report note)
      - napCleanupComplete + Month 1 (NAP win priority)
      - score === null (insufficient-data branch in WHERE WE STAND)
      - trend UP / DOWN / FLAT / baseline (different framings)
      - Months 1-3 vs 4-6 vs 7+ in WHERE WE STAND
      - Closing line that signs off in the authority_figure's name.
    Exports SECTION_NAMES + FORBIDDEN_WORDS so pdf-builder.js can split
    on the same canonical set of headers.

  narrative-engine.js — async generateNarrative(context)
    Direct call to https://api.anthropic.com/v1/messages (NOT through
    the Cloudflare Worker — the runner runs server-side in GitHub
    Actions, no browser to protect). Headers: x-api-key from
    process.env.ANTHROPIC_API_KEY, anthropic-version 2023-06-01.
    Body: model claude-sonnet-4-20250514, max_tokens 4000,
    temperature 0.7, plus the system + messages from
    buildNarrativePrompt. AbortController-driven 90s timeout. Reads
    response.content[*].text and concatenates the text blocks. Every
    failure path returns null + logs.

  pdf-builder.js — async buildPDF(narrative, context)
    1. splitSections() splits Claude's narrative on the 8 LITERAL
       section headers using a multi-line regex (each header anchored
       to its own line, case-sensitive).
    2. paragraphize() escapes HTML, splits on blank lines into <p>
       blocks, converts single newlines inside a block to <br>; falls
       back to "(Section not produced this month.)" when a slot is empty.
    3. populateTemplate() does naive {{TOKEN}} replacement on the
       templates/report.html string for BUSINESS_NAME, REPORT_PERIOD,
       AUTHOR_LINE, FOOTER_LINE, and the 8 SECTION_* tokens.
    4. Puppeteer (headless, --no-sandbox) renders to
       outputs/<client_id>_AI_Visibility_<YYYY-MM>.pdf with Letter
       size, 1" margins, printBackground:true (so the WIN callout's
       grey block renders), and a header/footer template that shows
       "<pageNumber> / <totalPages>" right-aligned.
    5. Returns { path, bytes, filename } on success; null on any
       failure. Helpers (splitSections, paragraphize, reportPeriodLabel,
       outputFilename, populateTemplate, SECTION_NAMES, TEMPLATE_PATH,
       OUTPUT_DIR) are exported for unit testing.

  templates/report.html
    Self-contained HTML/CSS — no CDNs, no external images. Cover at
    the top (24pt Helvetica title in dark charcoal, business name in
    Georgia, report period, "Prepared by <authority>, Founder — iTech
    Valet" italic, horizontal rule). OPENING flows from the cover
    without a header. Every other body section gets a 14pt ALL-CAPS
    Helvetica header, a thin grey rule, a Georgia body at 11pt /
    1.6 line height. WIN callout is an <aside> with #F5F5F5 background
    and a 3pt dark left border; body inside it is italic. CLOSING
    ends with a centered footer line above a top border. Page-break-
    before set on every body section except OPENING and the WIN
    callout (the WIN deliberately shares a page with WHAT THE AI
    PLATFORMS SAID when space allows). Page numbers come from
    Puppeteer's footerTemplate (right-aligned, 9pt Helvetica grey).

  sheet-reader.js — readAEOInventory(clientConfig) added
    Resolves columns BY NAME via the AEO_COLUMN_ALIASES map (accepts
    STATUS / Status / status, CLUSTER_NUMBER / Cluster_Number / Cluster
    Number, CLUSTER_NAME / Cluster_Name / Cluster Name / CLUSTER /
    Cluster, STRATEGIC_OBJECTIVE / Strategic_Objective / Strategic
    Objective). Groups data rows by parsed CLUSTER_NUMBER (rows with
    blank cluster numbers are dropped). Tracks per-cluster hasLive,
    name, and strategicObjective. Returns:
      - totalLiveNodes: count of rows where status === "live" (case-insensitive)
      - currentCluster: { name, number, articleCount, strategicObjective }
        where currentCluster = the highest-numbered cluster with at least
        one Live row; articleCount = ALL rows in that cluster regardless
        of status
      - nextCluster: { name, number, strategicObjective } where
        nextCluster = the lowest-numbered cluster with no Live rows
    Every failure path returns the empty inventory + logs.

  runner.js (fully end-to-end)
    - Computes reportPeriodLabel ("May 2026") alongside the internal
      "2026-05" form; progressionPhase ("Foundation" / "Building" /
      "Authority") based on clientMonth; isFirstMonth (clientMonth ===
      1); isQuarterlyMonth (clientMonth % 3 === 0); napCleanupComplete
      from clientConfig (default false if absent).
    - Step 1 now also calls sheet-reader.readAEOInventory and inlines a
      fetchDriveDocText helper (googleapis Drive v3) to read 4.3A +
      4.3B. PLACEHOLDER ids and missing env vars short-circuit to "".
      Google Docs export to text/plain via files.export; plain files
      use files.get?alt=media.
    - Step 6 assembles the full narrative context object (clientConfig,
      doc43A, doc43B, runDate, reportPeriod label, clientMonth,
      scoreResult, summaryHistory, narrativeRuns with Run 6 stripped,
      aeoData, napCleanupComplete, progressionPhase, isFirstMonth,
      isQuarterlyMonth) and calls narrative-engine.generateNarrative
      with that single argument.
    - Step 7 calls pdf-builder.buildPDF(narrative, context) and logs
      the resulting path + byte count.
    - Step 8 still typeof-guards drive-writer (last remaining scaffold).
    - Header documentation updated to reflect that ANTHROPIC_API_KEY
      is now consumed by both parser.js and narrative-engine.js.

  package.json — version 0.4.0; puppeteer ^23.0.0 added.

[ Session 5 — May 2026 ]

  TWO-CALL ARCHITECTURE LOCKED
    Analysis and writing are now separate cognitive jobs. The analysis
    call (Sonnet @ temp 0.3, max_tokens 3000) produces a structured
    Strategic Brief; the narrative call (Sonnet @ temp 0.7, max_tokens
    4000) reads the brief and writes the 8-section client-facing prose.
    The two calls run sequentially in the runner — analysis first, then
    narrative — so the writer has every framing decision pre-made.
    Analysis sees ALL 13 platform runs (including Run 6 — competitor
    intelligence is useful for the writer's frame, just never quoted
    to the client). Narrative only sees the 12 client-eligible runs.

  analysis-engine.js — generateAnalysis(context)
    Single Claude Sonnet call. Same direct path as narrative-engine
    (https://api.anthropic.com/v1/messages, x-api-key from
    process.env.ANTHROPIC_API_KEY). Model claude-sonnet-4-20250514,
    max_tokens 3000, temperature 0.3 (LOW — analysis, not creative
    writing). 90s AbortController timeout.

    System prompt locks the role ("senior data analyst for iTech
    Valet"), the contract ("Your output must be a single valid JSON
    object. No preamble. No markdown fences. Just the JSON."), and
    the honesty rule ("Do not manufacture wins. Do not hide problems.").

    User prompt assembles every fact the analyst needs: client
    identity + month, score block (current, prior, delta, trend,
    benchmark, subscores, platforms_available), full content
    inventory (totalLiveNodes + currentCluster + nextCluster), the
    nap_cleanup flag, all 13 run summaries, all 13 raw responses
    (each truncated to 2500 chars — these power ALIGNMENT SIGNAL
    detection), the full AI_Visibility_Summary history, and the
    citation history (URLs cited in 2+ distinct months). Closes with
    the Strategic Brief JSON schema verbatim.

    Output recovery: stripCodeFences (handles ```json wrappers) →
    direct JSON.parse → last-resort slice from first { to last }.
    validateStrategicBrief checks every top-level key is present.
    Any failure (auth error, network, malformed JSON, schema
    violation): return null + log. The runner forwards null to the
    narrative engine which has a documented "no brief" branch.

  sheet-reader.js — readCitationHistory(clientConfig) added
    Reads the full AI_Visibility_Raw tab and groups cited_urls by URL.
    Columns resolved by NAME via RAW_COLUMN_ALIASES (cited_urls,
    report_period, platform). Per row: split cited_urls on commas/
    semicolons, filter for http(s)://, dedupe within the row, then per
    unique URL bump months Set (report_period), platforms Set, total
    count. Final filter keeps only URLs whose months Set size >= 2 —
    single-month citations are noise, multi-month citations are the
    locked-in signal. Returns array sorted by times_cited desc then
    url asc. Months emitted as human labels ("April 2026") via a
    periodToLabel helper that matches reportPeriodLabel. Every failure
    path returns [] + logs.

  drive-writer.js — uploadPDF(pdfPath, clientConfig)
    googleapis Drive v3, same GOOGLE_SERVICE_ACCOUNT_JSON pattern as
    the Sheet modules. Scope: drive.file. PLACEHOLDER or missing
    drive_reports_folder_id short-circuits to null + a clear "skipping
    upload" log. Otherwise:
      1. Read the PDF as a Buffer.
      2. files.list with q "name = '<n>' and '<folder>' in parents
         and trashed = false" — finds an existing file with the same
         filename in the same folder. Escapes \\ and ' via escapeQueryString.
      3. If found: files.update with the new media body (stream from
         buffer). Returns { fileId, fileName, webViewLink, updated: true }.
      4. If not: files.create with parents=[folderId]. Returns
         { fileId, fileName, webViewLink, updated: false }.
    Any thrown error returns null + logs. supportsAllDrives is set on
    every call so Shared Drive folders work.

  index.html — VA-facing trigger UI
    Single self-contained HTML file. No external resources except
    system fonts. Two operator-config values at the top of the inline
    script: INTERNAL_ACCESS_KEY (placeholder "REPLACE_WITH_YOUR_KEY"
    the operator edits during deploy) and CLIENT_LIST (array of
    { label, client_id }). Click handler POSTs JSON
    { client_id, internal_key } to
    https://anthropic-proxy.gerek.workers.dev/trigger-report and ALSO
    sends the key as the x-internal-key header so either path on the
    Worker works. On 2xx: green "Report triggered" + button stays
    disabled (prevents double-fires). On non-2xx: red error with the
    HTTP status + a 140-char body excerpt. Pre-flight check prints a
    setup error if INTERNAL_ACCESS_KEY hasn't been replaced.

  .github/workflows/run-report.yml
    Two triggers: workflow_dispatch (with a client_id input) and
    repository_dispatch (event type "run-report" — the Worker fires
    this). Single job on ubuntu-latest, 30-min timeout,
    contents:write permission for the archive commit. Steps: checkout
    → setup-node 20 → npm install in AI-Visibility-Reporting →
    apt-get install all Puppeteer/Chromium system deps (libnss3,
    libgbm-dev, libgtk-3-0, etc.) → node runner.js --client=$CLIENT_ID
    with all six secrets injected as env → git add archive/ + commit
    + push when there are new files. CLIENT_ID resolves from
    github.event.inputs.client_id OR client_payload.client_id.

  prompts/narrative-prompt.js — Strategic Brief integration
    buildNarrativePrompt now reads analysisResult and repeatedCitations
    from the context. Adds two new sections to the user prompt:
      - REPEATED CITATIONS block — one bullet per URL cited in 2+ months
        (or a "(no repeated citations yet)" placeholder).
      - STRATEGIC BRIEF FROM ANALYSIS ENGINE block — full brief JSON
        pretty-printed PLUS a "How to use this brief" cheat sheet.
        When analysisResult is null, replaces the brief with a clear
        "Analysis engine unavailable this run" instruction directing
        the writer back to the original Month 1 / Month 2+ win priority
        order.
    A WIN WORTH NOTING section instructions rewritten: primary source
    is now recommended_wow_moment.callout_text (rewrite in Gerek's voice,
    keep every data point); when wins[] is empty, write the exact
    "This month's numbers are building the foundation. Next month we'll
    have more to show." sentence; original priority order survives as
    the explicit fallback when no brief is available.
    Exported formatStrategicBrief + formatRepeatedCitations for tests.

  runner.js — re-numbered to 13-step architecture
    Step 1  client config loaded (log only).
    Step 2  fetchDriveDocText for 4.3A + 4.3B.
    Step 3  readSheetHistory + readAEOInventory + readCitationHistory
            (the citation reader is the NEW call this session).
    Step 4  13 platform runs, serial, exponential-backoff retry.
    Step 5  parser.parseResponse for each (fused per-run with Step 4).
    Step 6  score-engine.calculateScore.
    Step 7  sheet-writer.writeToSheet.
    Step 8  github-archiver.archiveRun.
    Step 9  analysis-engine.generateAnalysis(analysisContext)  → NEW.
            analysisContext.runs is the FULL 13-row array (Run 6 included).
    Step 10 narrative-engine.generateNarrative(narrativeContext) →
            narrativeContext.runs is the 12-row filtered array (Run 6
            stripped); narrativeContext also carries analysisResult.
    Step 11 pdf-builder.buildPDF(narrative, narrativeContext).
    Step 12 drive-writer.uploadPDF(pdfPath, clientConfig)  → NEW.
            No more typeof guard — real call.
    Step 13 Completion log: client, period label, score, PDF path,
            Drive link, elapsed seconds. Single line, easy to grep.

  package.json — bumped to 0.5.0; @anthropic-ai/sdk ^0.40.0 added per
    Session 5 spec. The SDK is declared but not imported — parser,
    narrative, and analysis all use raw fetch. Reserved for any
    future module that prefers the SDK's tool-use / streaming helpers.

================================================

MANUAL SETUP — CLOUDFLARE WORKER /trigger-report ENDPOINT
---------------------------------------------------------

The existing Worker at anthropic-proxy.gerek.workers.dev needs a new
route added. This is a manual Cloudflare Worker code addition — not
automated by this tool.

New route: POST /trigger-report

Logic:
  1. Read x-internal-key header from the request.
  2. Compare against INTERNAL_ACCESS_KEY environment variable in the
     Worker.
  3. If mismatch: return 401.
  4. If match: read client_id from the request body (JSON).
  5. POST to GitHub API:
       https://api.github.com/repos/iTechValet/iTV-Tools/dispatches
     Headers:
       Authorization: Bearer ${GITHUB_PAT}
       Accept: application/vnd.github+json
       X-GitHub-Api-Version: 2022-11-28
       User-Agent: anthropic-proxy
     Body:
       { "event_type": "run-report",
         "client_payload": { "client_id": client_id } }
  6. Return 200 on success, 500 on GitHub API failure (forward the
     status + a body excerpt back to index.html so the VA sees the
     real reason in red).

Environment variables to add to the Worker:
  - INTERNAL_ACCESS_KEY   any secure string you choose (paired to
                          index.html's INTERNAL_ACCESS_KEY constant).
  - GITHUB_PAT            GitHub Personal Access Token with the "repo"
                          scope on iTechValet/iTV-Tools.

When ready, .github/workflows/run-report.yml consumes the GitHub
repository_dispatch with types: [run-report] — already wired on the
GitHub side; only the Worker route is missing.

================================================

WHAT IS PENDING — OPERATIONAL ONLY
----------------------------------

Phase 1 is feature-complete in code. The remaining work is operational:

  1. Cloudflare Worker /trigger-report route (manual — see above).
  2. GitHub Secrets provisioning:
       ANTHROPIC_API_KEY
       OPENAI_API_KEY
       GEMINI_API_KEY
       PERPLEXITY_API_KEY
       GROK_API_KEY
       GOOGLE_SERVICE_ACCOUNT_JSON
  3. Replace INTERNAL_ACCESS_KEY in index.html with the real value
     paired to the Worker, then deploy via GitHub Pages.
  4. Replace the PLACEHOLDER_* values in clients/itechvalet.json with
     the real Google Sheet ID, Drive folder ID, and 4.3A / 4.3B Doc
     IDs.
  5. Confirm the iTech Valet Google Sheet has the four required tabs
     (AEO_Inventory, AEO_Run_Data, AI_Visibility_Raw,
     AI_Visibility_Summary) — duplicate from the master template if not.
  6. Share the Google Sheet AND the Drive Monthly Reports folder with
     the service account email (read+write on the Sheet, read+write
     on the folder).
  7. First end-to-end test run — manually fire workflow_dispatch in
     the GitHub Actions UI with client_id=itechvalet, watch the logs.

After the first successful run, the same workflow works for any new
client: add their JSON to clients/, add them to CLIENT_LIST in
index.html, confirm their sheet has the four tabs, share the sheet +
folder with the service account.

================================================

THE 13 RUNS
-----------

ID  | Platform           | Signal | Prompt template
----|--------------------|--------|----------------------------------------------------------------------------------------
1A  | ChatGPT (gpt-4o)   |  1     | Who is the most trusted {service} {market_scope} for {primary_topic}?
1B  | Gemini             |  1     | (same as 1A, fresh session)
1C  | Perplexity (sonar) |  1     | (same as 1A, fresh session)
1D  | Grok               |  1     | (same as 1A, fresh session)
2   | ChatGPT            |  1+3   | Compare the authority and expertise of {top_competitor} vs. {business_name}
    |                    |        | {market_scope}. Who does AI trust more for {primary_topic}?
3   | Gemini             |  2     | Who is {authority_figure}, the {authority_title} of {business_name}? They run a
    |                    |        | {service} {market_scope}. What is this person's personal reputation and professional
    |                    |        | standing for {primary_topic}?
    |                    |        | (Session 3 fix — pre-naming the authority makes authority_figure_named measurable.)
4A  | ChatGPT            |  2     | What do people say about {business_name} as a {service}? What are the common reviews,
    |                    |        | complaints, and reasons people recommend or avoid them?
4B  | Perplexity         |  2     | (same as 4A — Perplexity surfaces review aggregators more aggressively)
5A  | ChatGPT            |  1     | I'm ready to open a {service} account. Who should I call first?
5B  | Grok               |  1     | (same as 5A)
6   | ChatGPT            |  3     | ⚠️ INTERNAL ONLY — NEVER in client deliverables, NEVER passed to Claude Sonnet.
    |                    |        | What are the main reasons people choose {top_competitor} for {primary_topic}? What do
    |                    |        | reviewers and {customer_term} say about them?
7A  | ChatGPT            |  3     | What sources or websites does AI commonly cite when answering questions about
    |                    |        | {primary_topic}? List the most frequently referenced sources.
7B  | Perplexity         |  3     | (same as 7A — Perplexity is citation-native)

Signal legend:
  1   "Are they being recommended?"  (the money signal)
  1+3 Head-to-head
  2   "How are they being described?"
  3   "What is blocking the recommendation?"

================================================

PARSER SPEC
-----------

Primary — Claude Haiku (model: claude-haiku-4-5-20251001) routed through
the Cloudflare Worker proxy at anthropic-proxy.gerek.workers.dev.

  System prompt:
    - Strict JSON only. No preamble. No markdown fences.
    - Schema is fixed. All fields required. Enums restricted.
    - Booleans must be true/false (not strings).
    - cited_urls is an array of explicit URLs (no reconstruction).

  Few-shot examples in the messages array (Anthropic convention):
    1. POSITIVE — brand recommended, ranked first, authority named.
    2. NEGATIVE — brand named but disrecommended, competitor preferred.
    3. MIXED   — brand mentioned with conflicting reviews + 2 URLs.

  User prompt body:
    Extract the following fields from this AI platform response.
    Business name being tracked: {business_name}
    Competitor being tracked: {top_competitor}

    Response to analyze:
    {raw_response}

    Return ONLY this JSON object with no other text: { ...schema... }

  Schema:
    brand_mentioned          boolean
    brand_recommended        boolean
    brand_position           "first" | "second" | "third" | "other" | "not_mentioned"
    competitor_mentioned     boolean
    competitor_recommended   boolean
    sentiment_signal         "positive" | "neutral" | "negative" | "mixed"
    cited_urls               string[]
    authority_figure_named   boolean

Fallback — heuristic regex (runs when Haiku errors, times out, returns
empty content, or returns malformed JSON):
  - brand_mentioned     case-insensitive substring of business_name.
  - brand_recommended   any of [recommend, call, contact, choose, trust,
                        best] within ±80 chars of a brand mention.
  - sentiment_signal    positive vs negative keyword counts →
                          both    → "mixed"
                          pos>0   → "positive"
                          neg>0   → "negative"
                          else    → "neutral"
  - Every other field defaults: false / "not_mentioned" / [].
  - parser_status set to "heuristic".

Threshold rule (enforced by runner.js after the 13-run loop, NOT by
parser.js):
  - Count rows with parser_status === "heuristic".
  - If >= 4: set heuristic_warning = true on the summary row.
  - The run STILL COMPLETES. Never stop the pipeline.

================================================

AI VISIBILITY SCORE — FORMULA (JS ONLY, NEVER CLAUDE)
-----------------------------------------------------

Composite 0–100, calculated by score-engine.js. The score engine is
pending build — this is the spec it will implement.

  Mention Rate     (40%) = brand_mentioned runs / brand-trackable runs × 100
  Recommendation   (35%) = brand_recommended runs / all runs × 100
  Sentiment Score  (25%) = positive-sentiment runs / all runs × 100

  Final Score = (mention_rate × 0.40)
              + (recommendation_rate × 0.35)
              + (sentiment_score × 0.25)

Trend arrow:
  current_score - prior_score from AI_Visibility_Summary (read by
  sheet-reader.js before the run starts).
    > 0   → UP
    < 0   → DOWN
    == 0  → FLAT

Benchmark ranges by client_month (displayed alongside the score):
  Months 1–3   Expected 10–25.   "Foundation building phase."
  Months 4–6   Target   20–40.   "Content compounding."
  Months 7–12  Goal     35–60+.  "Recommendation territory."

The score engine receives parsed rows and prior_score; it returns the
final score, trend, benchmark, and the three subscores. Claude never
calculates or interprets any of this.

================================================

GOOGLE SHEET STRUCTURE
----------------------

One sheet per client. Four tabs. All tool-written. Zero manual input.
Both tools target tabs by NAME — never by position. New client = duplicate
the master template (master template is a manual one-time setup).

  Tab                    | Written By               | Purpose
  -----------------------|--------------------------|--------------------------------------------------
  AEO_Inventory          | AEO Tool                 | Article production tracker
  AEO_Run_Data           | AEO Tool (future)        | Cost / time / performance per article run
  AI_Visibility_Raw      | THIS TOOL                | One row per platform run per month (13 rows/month)
  AI_Visibility_Summary  | THIS TOOL                | One row per month — composite score + trends

AI_Visibility_Raw row (13 per monthly run):
  run_id | client_id | platform | signal | run_date | report_period |
  client_month | brand_mentioned | brand_recommended | brand_position |
  competitor_mentioned | competitor_recommended | sentiment_signal |
  cited_urls | authority_figure_named | raw_response | response_length |
  platform_available | parser_status

AI_Visibility_Summary row (1 per monthly run):
  report_period | client_month | run_date | ai_visibility_score |
  prior_score | score_delta | trend_direction | benchmark_range |
  mention_rate | recommendation_rate | sentiment_score |
  platforms_available | heuristic_fallback_count

(The runner additionally stamps run_date, report_period, client_month,
heuristic_fallback_count, and heuristic_warning on the summary object
before the write — so even a partial score-engine output gets surfaced
to the Sheet.)

================================================

LOCKED DECISIONS (all 16 — from the build plan)
-----------------------------------------------

 1. One tool, all clients. Config file is the only thing that changes
    per client.
 2. JS calculates the score. Claude writes the story. Never cross this
    line.
 3. 13 runs, 4 platforms. Serial execution. Never parallel. Perplexity
    uses sonar model specifically.
 4. Exponential backoff. 5 retries. Never crash. Unavailable = log and
    continue.
 5. Four tabs per client sheet. All tool-written. Zero manual input.
    Tab names identical across all clients. Both tools target by name,
    never by position.
 6. GitHub is the backup. Raw JSON every run. PDFs go to Google Drive.
 7. Run 6 is internal only. Never in client deliverables. Never passed
    to Claude narrative.
 8. Claude receives full Sheet history every month. No arbitrary cutoff.
 9. Looker Studio is desktop only. Note in delivery email. Not built —
    manual setup after Phase 1.
10. Dashboard shows data. Report tells the story. Two jobs. Never overlap.
11. Dates are never calculated by Claude. Pass run_date, report_period,
    client_month as facts.
12. Execution runs in GitHub Actions — not the browser. Browser is
    trigger only. VA can close tab immediately.
13. Claude Haiku for parsing. Claude Sonnet for narrative. Two separate
    calls. Two separate jobs.
14. Parser fallback threshold: 4+ heuristic runs = ⚠️ flag + continue.
    Never stop the run.
15. Master Sheet template governs onboarding. New client = duplicate
    template. Never blank sheet.
16. Cowork is not part of this build. Phase 3 trigger is GitHub Actions
    cron. Cowork may be evaluated after 20+ successful runs.

================================================

KNOWN LIMITATIONS — DEFERRED TO LATER PHASE 1 SESSIONS
------------------------------------------------------

  - parser_status for unavailable runs uses the value "n/a" so the
    threshold rule (4+ heuristic) doesn't accidentally count empty
    rows. The schema in the build plan lists only "llm" | "heuristic" —
    "n/a" is an orchestrator-side extension limited to placeholder rows.

  - The Haiku parser user prompt still does NOT receive the authority
    figure's name — only business_name and top_competitor are passed
    into the parser system + user prompts. authority_figure_named is
    therefore parsed from whether the response itself names a specific
    human associated with the business. With the Session 3 Run 3 fix
    (which now puts {authority_figure} into the platform prompt), the
    platform's response is far more likely to echo the name back, so
    the field should now read true on Run 3 most of the time even
    without the parser knowing the exact string.

  - Cloudflare Worker /trigger-report route is NOT YET ADDED. Until
    it is (see "MANUAL SETUP" section above), index.html cannot
    actually trigger a run — pressing Run Report will hit the existing
    Worker, which doesn't know that route and will return 404 or
    forward to the Anthropic proxy path. The GitHub Actions
    workflow_dispatch entry point is unaffected — the operator can
    fire a run from the GitHub Actions UI immediately.

  - INTERNAL_ACCESS_KEY in index.html is the placeholder string
    "REPLACE_WITH_YOUR_KEY". The page's pre-flight check refuses to
    fire the trigger until the operator edits this value. Same
    placeholder needs to be set as an env var on the Worker side.

  - clients/itechvalet.json holds PLACEHOLDER_* values for every live
    Google ID (sheet_id, drive_reports_folder_id, doc_43A_id,
    doc_43B_id, looker_dashboard_id, client_email). The runner is
    coded to short-circuit gracefully on each (Drive doc fetch
    returns "", Drive upload logs "skipping", Sheet read returns
    defaults), but a meaningful first run requires real IDs.

  - First end-to-end run against a real sheet has not happened yet.
    Every module is unit-smoke-tested but the full Anthropic + Google
    + OpenAI/Gemini/Perplexity/Grok integration in CI is still to be
    exercised.

  - Puppeteer + Chromium download in the GitHub Actions runner: the
    .github/workflows/run-report.yml file does `npm install`, which
    triggers Puppeteer's post-install Chromium download. ubuntu-latest
    has internet access by default; the apt step also installs the
    Chromium system libraries (libnss3, libgbm-dev, libgtk-3-0, etc.).
    Ubuntu 22.04+ may rename libasound2 → libasound2t64; the workflow
    currently asks for libasound2 — if a CI run errors on this we'll
    update the package name.

================================================

CHANGE LOG
----------

Session 1 | May 2026 | Scaffold: full repo structure, iTech Valet client config, 13 prompt templates. 17 files, 525 lines.
Session 2 | May 2026 | Built: runner.config.js, runner.js (full pipeline orchestrator), parser.js (Haiku + heuristic fallback). Build plan corrected from 12 to 13 runs.
Session 3 | May 2026 | Fixed: authority_figure injected for Run 3, Perplexity citations array captured. Built: score-engine.js, sheet-reader.js, sheet-writer.js, github-archiver.js. runner.js wired through step 8.
Session 4 | May 2026 | Fixed: Cloudflare Worker 400 error in parser.js. Built: narrative-prompt.js (full 8-section report spec with emotional arc, alignment signal detection, Month 1 NAP flag, wow moment priority logic), narrative-engine.js (Claude Sonnet direct call), pdf-builder.js (Puppeteer), templates/report.html (Brighton Gold design reference). runner.js fully wired Sessions 1–4. AEO_Inventory reader added to runner. Pipeline complete from trigger to PDF generation.
Session 5 | May 2026 | Built: analysis-engine.js (Strategic Brief two-call architecture), citation history tracking in sheet-reader.js, drive-writer.js (PDF upload to Google Drive), index.html VA UI (Cloudflare dispatch trigger), .github/workflows/run-report.yml (GitHub Actions workflow). Runner fully wired Steps 1-13. Cloudflare Worker /trigger-report documented as manual setup step. Tool is feature-complete pending secrets setup and first test run.

================================================
END OF DOCUMENT
