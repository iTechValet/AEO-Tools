# AI Visibility Reporting Tool — Master Build Plan V1.1
**iTech Valet Internal Document**
Version: 1.1 | Updated: May 2026 | Status: Claude Code Ready
Governing Spec: AI_Visibility_Reporting_Tool_Master_Spec_V1.0.txt
Prompt File: 5.1_Monthly_AI_Visibility_Report_Master_Prompt_V1.8.txt
Architectural Model: AEO Article Creation Tool (proven pattern — extend, don't reinvent)

---

## HOW TO USE THIS DOCUMENT

This is the single source of truth for building the AI Visibility Reporting Tool with Claude Code.

**Start of every Claude Code session:**
> "Read the AI Visibility Tool Master Build Plan V1.1 before doing anything. Do not make assumptions — everything you need is in that document."

**Claude Code session rules (non-negotiable):**
Every session ends with:
1. Syntax check the file
2. Commit with a descriptive message
3. Push to GitHub
4. Update this Master Build Plan to log changes
5. Commit and push the Master Build Plan

---

## THE ONE THING THAT MATTERS

This tool is a client retention system disguised as reporting. It proves the program is working — especially in months 1–6 when churn risk is highest. Every architectural decision serves that goal.

**Architectural law inherited from AEO tool:** Code executes on fixed specs; Claude writes narrative only. Every generative responsibility moved from interpretation to execution improves reliability.

---

## ARCHITECTURAL MODEL — AEO TOOL

This tool is built on the same proven patterns as the AEO Article Creation Tool. Do not reinvent what already works.

**What carries over directly from the AEO tool:**
- Single HTML file hosted on GitHub Pages
- Cloudflare Worker proxy (`anthropic-proxy.gerek.workers.dev`) for Claude API calls — reuse as-is
- Google Drive fetch pattern for 4.3A and 4.3B by file ID — same key, same pattern
- Step-by-step progress display (green = done, spinner = running, red = real error only)
- JS owns all calculations — Claude writes prose only
- VA-facing UI — no exposed keys, no terminal, no code

**What is new (the actual build work):**
- 13 API calls to 4 external platforms (ChatGPT, Gemini, Perplexity, Grok)
- Claude Haiku parser with heuristic fallback
- Google Sheets writer via service account
- AI Visibility Score engine (JS only)
- Puppeteer PDF builder
- Google Drive write for PDF delivery
- GitHub Actions trigger mechanism (Phase 1: manual via Cloudflare dispatch proxy; Phase 3: cron)

---

## ARCHITECTURE OVERVIEW

```
CLIENT CONFIG (JSON, one per client)
        ↓
VA TRIGGERS RUN (index.html → Cloudflare Worker dispatch → GitHub Action)
        ↓
RUNNER (Node.js in GitHub Actions — orchestrates everything)
        ↓
GOOGLE DRIVE FETCH (4.3A + 4.3B — same pattern as AEO tool)
        ↓
SHEET READER (reads full AI_Visibility_Raw history for trend calculation)
        ↓
13 API CALLS — serial, one at a time (ChatGPT, Gemini, Perplexity/sonar, Grok)
        ↓
CLAUDE HAIKU PARSER per run (strict JSON schema + heuristic fallback)
        ↓
SCORE ENGINE (JS calculates AI Visibility Score — never Claude)
        ↓
GOOGLE SHEET WRITER (AI_Visibility_Raw + AI_Visibility_Summary tabs)
        ↓
GITHUB ARCHIVER (raw JSON backup — [YYYY-MM]/[client_id]_raw.json)
        ↓
CLAUDE SONNET NARRATIVE ENGINE (report written in Gerek's voice)
        ↓
PUPPETEER PDF BUILDER (named [client_id]_AI_Visibility_[YYYY-MM].pdf)
        ↓
GOOGLE DRIVE WRITER (PDF → client's "Monthly Reports" folder)
        ↓
[PHASE 3] EMAIL DELIVERY (PDF link, simple template)
        ↓
LOOKER STUDIO DASHBOARD (manual setup — reads Sheet automatically)
```

**Key architectural laws:**
- The AI Visibility Score is calculated by JS. Claude NEVER calculates or interprets the score.
- Claude receives the score, trend, and benchmark as pre-calculated facts and writes narrative around them.
- Dates are never calculated by Claude. Pass `run_date`, `report_period`, and `client_month` as facts.
- API calls are serial. Never parallel. Clean error isolation per run.

---

## REPO STRUCTURE

```
github.com/iTechValet/iTV-Tools
└── AI-Visibility-Reporting/
    ├── index.html                 # VA-facing UI — select client, trigger run, view progress
    ├── runner.js                  # Main Node.js orchestrator (runs in GitHub Actions)
    ├── runner.config.js           # Platform API configs, retry settings, timeouts
    ├── parser.js                  # Claude Haiku parser + heuristic fallback
    ├── score-engine.js            # AI Visibility Score calculator (JS only — never Claude)
    ├── sheet-reader.js            # Reads Sheet history before run starts
    ├── sheet-writer.js            # Writes to AI_Visibility_Raw + AI_Visibility_Summary
    ├── github-archiver.js         # Saves raw JSON to archive folder
    ├── narrative-engine.js        # Claude Sonnet API call — report generation only
    ├── pdf-builder.js             # Puppeteer — HTML template → PDF
    ├── drive-writer.js            # Uploads PDF to client Google Drive folder
    ├── prompts/
    │   ├── prompt-templates.js    # 12 run prompt templates — variables injected at runtime
    │   ├── parser-prompt.js       # Claude Haiku parser prompt — strict JSON schema
    │   └── narrative-prompt.js    # Claude Sonnet report generation prompt
    ├── templates/
    │   └── report.html            # PDF HTML template — Puppeteer renders this
    ├── clients/
    │   ├── brighton_gold.json     # Brighton Gold — first test client
    │   └── [client_id].json       # One file per client — swap config = swap client
    └── archive/
        └── [YYYY-MM]/
            └── [client_id]_raw.json   # Monthly raw JSON backup per client
```

**Hosting:** `itechvalet.github.io/iTV-Tools/AI-Visibility-Reporting/`
**Cloudflare proxy:** `anthropic-proxy.gerek.workers.dev` — reused from AEO tool for Claude calls + GitHub Action dispatch trigger

---

## CLIENT CONFIG STRUCTURE

One JSON file per client. Swap the config = swap the client. Codebase never changes.

```json
{
  "client_id": "brighton_gold",
  "business_name": "Brighton Gold",
  "service": "Gold IRA dealer",
  "primary_topic": "Gold IRA rollovers for retirement",
  "market_scope": "for Americans planning retirement",
  "top_competitor": "Augusta Precious Metals",
  "authority_figure": "Nathaniel Cross",
  "authority_title": "Director of Research",
  "customer_term": "customers",
  "contract_start": "2026-01-01",
  "sheet_id": "[Google Sheet ID]",
  "drive_reports_folder_id": "[Google Drive folder ID — Monthly Reports]",
  "looker_dashboard_id": "[Looker Studio ID]",
  "client_email": "[client email]",
  "doc_43A_id": "[Google Drive file ID — universal 4.3A]",
  "doc_43B_id": "[Google Drive file ID — client 4.3B]",
  "run_cadence": "monthly"
}
```

**Notes:**
- `client_month` calculated at runtime from `contract_start` vs current date — never hardcoded
- `report_month` set dynamically at runtime — never hardcoded
- New client = new JSON file. Zero codebase changes required.
- `looker_dashboard_id` populated after Phase 2 Looker setup — leave blank in Phase 1

---

## GOOGLE SHEET STRUCTURE

**One sheet per client. Four tabs. All tool-written. Zero manual input.**

Every client sheet is created from the master template. Tab names are identical across all clients. Both tools target tabs by name — never by position.

| Tab Name | Written By | Purpose |
|---|---|---|
| `AEO_Inventory` | AEO Tool | Article production tracker (renamed from Sheet1) |
| `AEO_Run_Data` | AEO Tool (future upgrade) | Cost, time, performance per article run |
| `AI_Visibility_Raw` | This tool | One row per run per month (13 rows/month) |
| `AI_Visibility_Summary` | This tool | One row per month — calculated scores and trends |

**Master Template:** One Google Sheet template with all 4 tabs pre-named. New client onboarding = duplicate the template. Never create a blank sheet.

**Pre-build dependency:** The AEO tool must be updated to target `AEO_Inventory` by tab name before this tool ships. See AEO Tool Update section below.

### AI_Visibility_Raw — Row Structure (one row per run):
```
run_id | client_id | platform | signal | run_date | report_period |
client_month | brand_mentioned | brand_recommended | brand_position |
competitor_mentioned | competitor_recommended | sentiment_signal |
cited_urls | authority_figure_named | raw_response | response_length |
platform_available | parser_status
```

### AI_Visibility_Summary — Row Structure (one row per month):
```
report_period | client_month | run_date | ai_visibility_score |
prior_score | score_delta | trend_direction | benchmark_range |
mention_rate | recommendation_rate | sentiment_score |
platforms_available | heuristic_fallback_count
```

---

## THE 12 RUNS — PROMPT ARCHITECTURE

Three signals. Thirteen runs. All variables injected from client config at runtime.

### Signal 1 — Are They Being Recommended? (The money signal)

| Run | Platform | Prompt Template |
|-----|----------|----------------|
| 1A | ChatGPT | "Who is the most trusted {service} {market_scope} for {primary_topic}?" |
| 1B | Gemini | Same prompt. Fresh session. |
| 1C | Perplexity (sonar) | Same prompt. Fresh session. |
| 1D | Grok | Same prompt. Fresh session. |
| 5A | ChatGPT | "I'm ready to open a {service} account. Who should I call first?" |
| 5B | Grok | Same prompt. |

### Signal 1 + 3 — Head-to-Head

| Run | Platform | Prompt Template |
|-----|----------|----------------|
| 2 | ChatGPT | "Compare the authority and expertise of {top_competitor} vs. {business_name} {market_scope}. Who does AI trust more for {primary_topic}?" |

### Signal 2 — How Are They Being Described?

| Run | Platform | Prompt Template |
|-----|----------|----------------|
| 3 | Gemini | "Who is the recognized expert or authority figure behind {business_name}? They are a {service} {market_scope}. What is this person's personal reputation and professional standing?" |
| 4A | ChatGPT | "What do people say about {business_name} as a {service}? What are the common reviews, complaints, and reasons people recommend or avoid them?" |
| 4B | Perplexity | Same prompt. Perplexity surfaces review aggregators more aggressively — different signal. |

### Signal 3 — What Is Blocking the Recommendation? (INTERNAL ONLY — never in client deliverables)

| Run | Platform | Prompt Template |
|-----|----------|----------------|
| 6 | ChatGPT | "What are the main reasons people choose {top_competitor} for {primary_topic}? What do reviewers and {customer_term} say about them?" |
| 7A | ChatGPT | "What sources or websites does AI commonly cite when answering questions about {primary_topic}? List the most frequently referenced sources." |
| 7B | Perplexity | Same prompt. Citation-native — strongest signal for this run. |

---

## RESPONSE PARSER — CLAUDE HAIKU + HEURISTIC FALLBACK

**Primary:** Claude Haiku (`claude-haiku-4-5-20251001`) extracts structured fields from each raw response.

**Parser prompt architecture (strict JSON enforcement):**
- System prompt specifies rigid JSON schema
- Three few-shot examples: one clear positive, one clear negative, one ambiguous/mixed
- Explicit enum lists for every enum field (e.g., `sentiment_signal: ["positive", "neutral", "negative", "mixed"]`)
- Zero conversational filler permitted in output
- JSON mode enforced

**Fields extracted per run:**
```js
{
  brand_mentioned: boolean,
  brand_recommended: boolean,
  brand_position: "first" | "second" | "third" | "other" | "not_mentioned",
  competitor_mentioned: boolean,
  competitor_recommended: boolean,
  sentiment_signal: "positive" | "neutral" | "negative" | "mixed",
  cited_urls: string[],
  authority_figure_named: boolean,   // Run 3 only
  parser_status: "llm" | "heuristic"
}
```

**Fallback:** If Haiku call fails or returns malformed JSON, heuristic regex attempts the 3 score-critical fields only:
- `brand_mentioned` — business name present in response text
- `brand_recommended` — recommendation language present near business name
- `sentiment_signal` — positive/negative keyword matching

**Threshold rule:** If 4 or more runs in a single month log `parser_status: "heuristic"`, the tool completes the run but adds a ⚠️ flag to the PDF and Sheet row. VA delivers the report and alerts Gerek that parser maintenance is needed. Run does not stop.

---

## AI VISIBILITY SCORE ENGINE (JS ONLY — NEVER CLAUDE)

Composite score 0–100. Calculated entirely in `score-engine.js`. Claude never touches this math.

```
Mention Rate     (40%) = brand_mentioned runs / brand-trackable runs × 100
Recommendation   (35%) = brand_recommended runs / all runs × 100
Sentiment Score  (25%) = positive sentiment runs / all runs × 100

Final Score = (mention_rate × 0.40) + (recommendation_rate × 0.35) + (sentiment_score × 0.25)
```

**Trend arrow:** Current month score minus prior month score. Pulled from `AI_Visibility_Summary` tab by `sheet-reader.js` before the run starts. JS calculates — no human involvement.

**Benchmark ranges by client_month (displayed alongside score):**
- Months 1–3: Expected 10–25. "Foundation building phase."
- Months 4–6: Target 20–40. "Content compounding."
- Months 7–12: Goal 35–60+. "Recommendation territory."

---

## RETRY LOGIC (ALL PLATFORMS)

- 5 attempts per run with exponential backoff (not fixed 30-second intervals)
- Backoff schedule: 30s → 60s → 120s → 240s → 480s
- On failure after 5 attempts: log `platform_available: false`, write "data unavailable" to Sheet, continue
- Never crash. Never require human intervention. Never a single point of failure.
- Grok is the most likely to be unavailable — graceful fallback is mandatory for Runs 1D and 5B

---

## CLAUDE NARRATIVE ENGINE (CLAUDE SONNET — PROSE ONLY)

Claude receives exactly this context. Nothing more, nothing less.

**Inputs passed to Claude:**
1. All raw responses from current month's 13 runs (raw_response fields — labeled by run_id)
2. Full AI_Visibility_Summary history — all prior months (no arbitrary cutoff)
3. AI Visibility Score for this month (pre-calculated by JS)
4. Score last month (read from Sheet by sheet-reader.js)
5. Trend direction — UP / DOWN / FLAT (pre-calculated by JS)
6. Benchmark range for this client_month (pre-calculated by JS)
7. client_month integer
8. run_date and report_period (never let Claude calculate dates)
9. 4.3A content (fetched from Google Drive at runtime)
10. 4.3B content (fetched from Google Drive at runtime)

**Claude's only job:** Write the narrative. All numbers, scores, dates, trends, and benchmarks arrive as pre-calculated facts.

**Report sections (in order):**
1. Executive Summary — 2–3 sentences. The verdict.
2. What We Did This Month — content, milestones, actions. Concrete. Numbers.
3. What the AI Platforms Said — one paragraph per platform. Framed against last month.
4. Where We Stand — score, trend, benchmark context.
5. What's Coming Next — one specific next action. Justifies the retainer.
6. On Our Radar — one forward signal to watch.
7. Closing — Gerek Allen, Founder — iTech Valet signature block.

**Run 6 (competitor intelligence) is NEVER passed to Claude's narrative prompt. Sheet only.**

**Voice rules (from 4.3A and 5.1 Master Prompt — non-negotiable):**
- 5th grade reading level. Casual, direct, warm. Never corporate.
- Short paragraphs. One idea per paragraph. No sentence over 20 words.
- Active voice only.
- Forbidden terms: deployed, clusters, intent layers, pillars, semantic, entity, trunk, branch, schema, backlink, keyword, SEO, Tier 1/2/3.
- No competitor names by name — "the top name in your market" only.
- Drift Detection: Replace {business_name} with a competitor. If it still reads accurate, rewrite.

---

## PDF BUILDER

- Library: Puppeteer (Node.js)
- Design reference: Brighton Gold April 2026 Report (4-page format)
- HTML template populated by JS from Claude's narrative output — Claude outputs text only, HTML structure is owned by the template
- Output filename: `[client_id]_AI_Visibility_[YYYY-MM].pdf`
- Uploaded to client's Google Drive "Monthly Reports" folder via service account
- No manual steps. No Word doc intermediate. PDF is the final output.

---

## VA-FACING UI (index.html)

Single HTML file. Hosted on GitHub Pages. Same pattern as AEO tool.

**UI flow:**
1. VA opens `index.html`
2. Selects client from dropdown (populated from `/clients/*.json` files)
3. Hits "Run Report"
4. UI sends POST to Cloudflare Worker `/trigger-report` endpoint with `client_id` + internal access key
5. Worker validates access key, fires `repository_dispatch` to GitHub with `client_id` as payload
6. GitHub Action picks up the event and runs `runner.js --client=[client_id]`
7. VA's browser shows "Run triggered — check back in ~10 minutes" confirmation
8. VA can close the tab immediately — execution is on GitHub's servers, not the browser
9. On completion, PDF is in Google Drive. VA downloads and sends to client.

**UI/UX rules (inherited from AEO tool):**
- Green = good/expected
- Red = real problem only — never use red for expected or normal states
- Progress display shows each step as it completes

**VA never enters API keys. Ever.**

---

## API KEY MANAGEMENT

| Key | Storage | Access Pattern |
|-----|---------|---------------|
| OpenAI (GPT-4o) | GitHub Secrets | runner.js calls directly from GitHub Actions |
| Google Gemini | GitHub Secrets | runner.js calls directly |
| Perplexity (sonar) | GitHub Secrets | runner.js calls directly |
| xAI (Grok) | GitHub Secrets | runner.js calls directly — graceful fallback if unavailable |
| Google Service Account JSON | GitHub Secrets | sheet-writer.js + drive-writer.js |
| Google Drive API key | Existing — restricted to itechvalet.github.io | Reuse from AEO tool for 4.3A/4.3B fetch |
| Claude API | GitHub Secret (Anthropic key) | narrative-engine.js calls directly from Actions |
| GitHub PAT (repo scope) | Cloudflare Worker environment variable | Worker fires repository_dispatch — never in browser |
| Internal Access Key | Cloudflare Worker environment variable | Validates VA trigger requests — never in browser |

**The Cloudflare Worker's new role:** Dispatch proxy only. VA browser POSTs to Worker → Worker validates access key → Worker fires GitHub `repository_dispatch` → Worker returns confirmation to browser. All heavy execution happens in GitHub Actions.

---

## PHASE BUILD ORDER

### PRE-BUILD — AEO Tool Update (must complete first)
- Update AEO tool to target `AEO_Inventory` tab by name instead of position
- Verify AEO tool writes correctly when additional tabs exist in the same sheet
- Create the master Google Sheet template with all 4 tabs pre-named
- Test with Brighton Gold sheet

### PHASE 1 — MVP (VA-Triggered)
Target: Working end-to-end on Brighton Gold config.

**Build order:**
1. Repo structure and file scaffolding
2. Cloudflare Worker update — add `/trigger-report` dispatch endpoint
3. GitHub Actions workflow file — `workflow_dispatch` + `repository_dispatch` triggers
4. Brighton Gold client config JSON
5. Prompt templates (13 runs, variables injected from config)
6. Platform runners — serial loop, exponential backoff retry logic
7. Claude Haiku parser — strict JSON schema prompt + heuristic fallback
8. Sheet reader — reads AI_Visibility_Summary history before run
9. Score engine — composite score, trend, benchmark
10. Sheet writer — AI_Visibility_Raw (13 rows) + AI_Visibility_Summary (1 row)
11. GitHub archiver — raw JSON per run
12. Claude Sonnet narrative engine — report generation
13. Puppeteer PDF builder + HTML report template
14. Google Drive writer — PDF upload to client folder
15. VA-facing index.html — client select, trigger, confirmation display
16. End-to-end test run on Brighton Gold

**Phase 1 does NOT include:**
- Email delivery (VA manually sends PDF — downloads from Google Drive)
- Looker Studio setup (manual — done after first successful run)
- Multi-client loop (single client per trigger in Phase 1)
- Cron automation (VA triggers manually in Phase 1)

### PHASE 2 — DASHBOARD (Manual Setup — Not Coded)
1. Looker Studio template connected to Brighton Gold AI_Visibility_Summary tab
2. All dashboard panels confirmed rendering correctly
3. Score benchmark ranges displaying correctly
4. Template duplicated for second client — confirm it works
5. Looker dashboard ID added to client JSON config

### PHASE 3 — FULL AUTOMATION
1. GitHub Actions cron job (1st of each month, 9 AM PT)
2. Multi-client loop — runner iterates through all active client JSON files
3. Atomic client execution — each client commits its own data before moving to the next
4. 60-second delay between clients to avoid rate limit clashes at scale
5. Gmail API email delivery — PDF Drive link attached, simple template
6. Run summary emailed to Gerek after each batch
7. Zapier webhook alert if any client fails all retries (fires POST → Zapier → email/SMS)

---

## LOCKED DECISIONS

These are final. Do not re-litigate in build sessions.

1. **One tool, all clients.** Config file is the only thing that changes per client.
2. **JS calculates the score. Claude writes the story.** Never cross this line.
3. **13 runs, 4 platforms. Serial execution.** Never parallel. Perplexity uses sonar model specifically.
4. **Exponential backoff. 5 retries. Never crash.** Unavailable = log and continue.
5. **Four tabs per client sheet. All tool-written. Zero manual input.** Tab names identical across all clients. Both tools target by name, never by position.
6. **GitHub is the backup.** Raw JSON every run. PDFs go to Google Drive.
7. **Run 6 is internal only.** Never in client deliverables. Never passed to Claude narrative.
8. **Claude receives full Sheet history every month.** No arbitrary cutoff.
9. **Looker Studio is desktop only.** Note in delivery email. Not built — manual setup after Phase 1.
10. **Dashboard shows data. Report tells the story.** Two jobs. Never overlap.
11. **Dates are never calculated by Claude.** Pass run_date, report_period, client_month as facts.
12. **Execution runs in GitHub Actions — not the browser.** Browser is trigger only. VA can close tab immediately.
13. **Claude Haiku for parsing. Claude Sonnet for narrative.** Two separate calls. Two separate jobs.
14. **Parser fallback threshold: 4+ heuristic runs = ⚠️ flag + continue.** Never stop the run.
15. **Master Sheet template governs onboarding.** New client = duplicate template. Never blank sheet.
16. **Cowork is not part of this build.** Phase 3 trigger is GitHub Actions cron. Cowork may be evaluated after 20+ successful runs.

---

## LOOKER STUDIO DASHBOARD (POST-PHASE-1 — MANUAL SETUP)

Not a build task. Manual setup after first successful run populates the Sheet.

**Setup:** Connect Looker Studio to `AI_Visibility_Summary` tab. ~3 clicks. One template per client via unique shared link. No client login required.

**Dashboard panels (built manually in Looker):**
1. AI Visibility Score — hero number with trend arrow and benchmark range
2. Mention Rate Trend — line chart, full history
3. Recommendation Rate Trend — line chart, full history
4. Platform Coverage — covered / not covered / unavailable per platform
5. Sentiment Signal — % positive / neutral / negative
6. Brand Position — first / second / third / other when mentioned

**Design principles:**
- One hero number. Big. Bold. Front and center.
- 5th grade English on every label. No jargon.
- Every panel has one sentence of context copy explaining why it matters.
- Benchmark range displayed next to score always.
- Desktop only. Mobile not supported.

---

## PRE-BUILD DEPENDENCY — AEO TOOL TAB UPDATE

**This must be completed before the AI Visibility tool build begins.**

The AEO tool currently writes to `Sheet1` (default tab, targeted by position). When a second tab is added to the client sheet, this will break.

**Required fix:**
- AEO tool targets `AEO_Inventory` tab by name
- AEO tool continues to function correctly when additional tabs exist
- Fix is surgical — does not affect any other AEO tool functionality

See separate Claude Code prompt below.

---

## RELATIONSHIP TO 5.1 MASTER PROMPT (V1.8)

The 5.1 Master Prompt is the current manual workflow. This tool automates it.

| 5.1 Manual Step | Tool Automation |
|----------------|-----------------|
| VA opens incognito windows, pastes 6 prompts manually | 13 platform runners fire via API automatically |
| VA pastes responses into working doc | Parser extracts all fields per run automatically |
| Claude analyzes + asks 4–5 interview questions | Score engine + structured Sheet history replaces the interview |
| VA answers Claude's questions | All context pre-assembled from config + Sheet |
| Claude writes Phase 1 report content | Claude Sonnet narrative engine — single structured call |
| Claude builds .docx + .txt files | Puppeteer generates PDF directly — uploaded to Drive |

**The 5.1 prompt is NOT replaced.** It remains for manual use. This tool is the automated version of the same workflow.

---

## CHANGE LOG

| Version | Date | Changes |
|---------|------|---------|
| V1.0 | May 2026 | Initial master build plan. Full architecture, phase plan, Gemini review questions. |
| V1.1 | May 2026 | Major update after two rounds of Gemini architectural review. Changes: AEO tool as architectural model locked; GitHub Actions as execution environment (browser is trigger only); Cloudflare Worker repurposed as dispatch proxy; serial execution + exponential backoff locked; Claude Haiku parser + heuristic fallback locked; PDFs to Google Drive (not GitHub); 4-tab Sheet structure finalized (AEO_Inventory, AEO_Run_Data, AI_Visibility_Raw, AI_Visibility_Summary); master Sheet template requirement added; Cowork removed from Phase 3; pre-build AEO tab fix added as dependency; Looker Studio confirmed as manual post-Phase-1 setup; full repo structure updated; locked decisions expanded to 16. |
| V1.2 | 2026-05-14 | Session 1 build log. Scaffolded the full AI-Visibility-Reporting/ folder per the REPO STRUCTURE section: 10 root-level JS files (runner, runner.config, parser, score-engine, sheet-reader, sheet-writer, github-archiver, narrative-engine, pdf-builder, drive-writer), 3 prompt files (prompts/prompt-templates.js, prompts/parser-prompt.js, prompts/narrative-prompt.js), templates/report.html, index.html, archive/.gitkeep. Every JS and HTML shell carries a top comment block stating what it does, what calls it, and what it calls. Built clients/itechvalet.json as the first test-client config (iTech Valet itself) — all live API/Sheet/Drive IDs left as PLACEHOLDER_* per the locked-decisions rule. Implemented prompts/prompt-templates.js with all 13 run-ID templates (1A, 1B, 1C, 1D, 2, 3, 4A, 4B, 5A, 5B, 6, 7A, 7B); single exported function getPrompt(runId, clientConfig); zero hardcoded client data; Run 6 flagged INTERNAL ONLY in code comments. All 13 JS files pass `node --check`; itechvalet.json validates; getPrompt smoke-tested against all 13 run IDs with iTech Valet config (no leftover tokens) plus unknown-runId and missing-variable guards. |
| V1.3 | 2026-05-14 | Session 2 build log. Run-count correction: every "12 runs" / "Twelve runs" / "12 API calls" / "12 rows" / "12 platform runners" reference in this doc is now "13" — the build plan table lists 13 explicit run IDs and Run 6 is intentional (INTERNAL ONLY, never client-facing). Built: runner.config.js (4 platforms: openai gpt-4o, gemini gemini-1.5-pro, perplexity sonar [non-negotiable], grok grok-3; retry maxAttempts=5 with backoff [30s,60s,120s,240s,480s]; timeoutMs=30s; secretKey is env-var NAME, never the value). runner.js (entry point for GitHub Actions, --client flag parse; per-step ISO-timestamped logging; pre-flight env-key check that marks platforms unavailable without crashing; 13-run SERIAL loop with full 5-attempt exponential-backoff retry per run; on full failure → placeholder row with platform_available:false, parser_status:"n/a"; AbortController timeout; Gemini and OpenAI-compatible code paths; threshold check sets heuristic_warning when 4+ rows go heuristic; Run 6 stripped before narrative call; every downstream call wrapped in try/catch so scaffolded modules don't crash the pipeline). parser.js (async parseResponse(rawResponse, clientConfig): Haiku via Cloudflare Worker proxy at anthropic-proxy.gerek.workers.dev/v1/messages with model claude-haiku-4-5-20251001; output recovery strips code fences and last-resort slices first '{' to last '}'; normalizeFields enforces strict booleans, enum lists, and string[] for cited_urls; heuristic fallback covers the 3 score-critical fields per spec with everything else defaulted; parser_status set to "llm" or "heuristic"). prompts/parser-prompt.js (SYSTEM_PROMPT with schema + enum enforcement + no-preamble rule; buildMessages returns { system, messages } where messages contains 3 few-shot user/assistant pairs — positive / negative / mixed — followed by the live user prompt in the exact structure from the Session 2 spec). All touched JS files pass `node --check`; smoke tests confirm: 4 platforms wired with perplexity model "sonar", retry/timeout values match spec; parser-prompt buildMessages returns 7 messages with alternating roles; heuristicParse correctly tags positive/negative/absent cases; normalizeFields coerces invalid types to safe defaults; runner --client parsing handles both `=` and space forms; calcClientMonth returns 5 for iTech Valet contract start (2026-01-01) vs today; all 13 RUN_IDS map to a platform and a signal. New Master Doc created at Docs/AI_Visibility_Reporting_Tool_Master_Doc_V1_0.txt mirroring the AEO Master Doc structure. |
| V1.4 | 2026-05-14 | Session 3 build log. Fixed two Session 2 known limitations: (1) Run 3 prompt template in prompts/prompt-templates.js now reads "Who is {authority_figure}, the {authority_title} of {business_name}? They run a {service} {market_scope}. What is this person's personal reputation and professional standing for {primary_topic}?" — pre-naming the authority so the platform's response actually echoes it back, making authority_figure_named measurable; REQUIRED_VARS expanded to enforce both new fields. (2) Perplexity citations: runner.callPlatform now returns { text, citations } for every platform (citations is [] for non-Perplexity), the main loop forwards citations to parser.parseResponse(rawResponse, clientConfig, citations), and the parser overrides cited_urls with the platform's authoritative citations whenever the array is non-empty (works on both the Haiku-success and heuristic-fallback paths). Built four modules: score-engine.js (pure JS calculateScore(runs, priorScore): filters to platform_available:true; subscores at 40/35/25 weights rounded to 1 decimal; insufficient_data:true with score:null when 0 platforms available; trend baseline/UP/DOWN/FLAT; benchmark tiers by client_month; returns prior_score + heuristic_fallback_count for the sheet write); sheet-reader.js (readSheetHistory(clientConfig) via googleapis service-account auth from process.env.GOOGLE_SERVICE_ACCOUNT_JSON; targets AI_Visibility_Summary by NAME; returns { priorScore, summaryHistory }; every failure path returns defaults + logs, never crashes); sheet-writer.js (writeToSheet with same auth; both Raw + Summary tabs checked by NAME; missing tab throws "Tab \"X\" not found in sheet Y. Ensure this sheet was created from the master template."; duplicate-month protection scans Raw for client_id+report_period match and skips both writes when found; booleans rendered as TRUE/FALSE; cited_urls comma-joined; raw_response truncated to 50,000 chars; heuristic_warning derived from heuristic_fallback_count>=4; Run 6 IS written to Raw — internal data only, stripped only at the narrative boundary); github-archiver.js (archiveRun(clientId, reportPeriod, runs, scoreResult) writes archive/<YYYY-MM>/<client_id>_raw.json to the local fs; YYYY-MM derived from reportPeriod string not new Date(); recursive mkdir; never crashes the pipeline on backup failure; the Actions workflow handles the git commit + push). Added package.json declaring googleapis ^144.0.0 + node engines >=18 + "npm start" alias for "node runner.js". Wired runner.js Step 1 to call sheet-reader.readSheetHistory and log priorScore + history length; Step 3 to call score-engine.calculateScore and log the resulting score/trend/delta/platforms_available/heuristic count; Step 4 to call sheet-writer.writeToSheet with the full (clientConfig, rows, scoreResult, period, runDate, clientMonth) signature and log append counts or skipped reason; Step 5 to call github-archiver.archiveRun and log the written path + bytes; Step 6 still typeof-guards narrative-engine but now passes scoreResult and summaryHistory. Verified: every JS file passes node --check; smoke tests pass for Run 3 prompt + REQUIRED_VARS guards, parser citations override (no citations / valid array / dirty array filtered), score-engine math + benchmarks + UP/DOWN/FLAT trends + heuristic count, github-archiver clean write + bad-period guard + bad-client-id guard. With googleapis mocked: sheet-reader returns priorScore from last row; sheet-writer rawRow is 19 cols with raw_response truncated to 50k and booleans rendered TRUE/FALSE; summaryRow is 17 cols with heuristic_warning auto-set when count>=4; duplicate-month scan correctly skips both writes; missing-tab throws the exact specified error. Side observation surfaced during testing: the existing anthropic-proxy.gerek.workers.dev Worker currently returns HTTP 400 "Missing x-api-key header" — the Worker is NOT injecting the Anthropic key as parser.js assumes. The pipeline still runs (heuristic fallback fires) but this needs to be fixed in Session 4+ before the Sonnet narrative call lands. Master Doc bumped to V1.1. |
| V1.5 | 2026-05-14 | Session 4 build log. Fixed the Cloudflare Worker 400 surfaced in Session 3: parser.js now reads process.env.ANTHROPIC_API_KEY at call time and sends it as the x-api-key header — the Worker is a pass-through, not an injecting proxy. Built four components: (1) prompts/narrative-prompt.js — exports buildNarrativePrompt(context) returning { system, messages } shaped for the Anthropic Messages API. System prompt pulls voice/identity rules from 4.3A with a graceful fallback when 4.3A is empty, locks a 19-word forbidden list (deployed, clusters, intent layers, pillars, semantic, entity, trunk, branch, schema, backlink, keyword, SEO, Tier 1/2/3, synergy, leverage, optimize, utilize), suppresses the competitor name ("the top name in your market"), enforces score-with-benchmark phrasing, locks the 8 ALL-CAPS section headers (OPENING, WHAT WE PUBLISHED THIS MONTH, WHAT THE AI PLATFORMS SAID, A WIN WORTH NOTING, WHERE WE STAND, ON OUR RADAR, THE BIGGER PICTURE, CLOSING), forbids markdown headers, and bans "just wait" + "as expected" framings without explanation. User prompt assembles pre-calculated facts (run_date, periodLabel, clientMonth, progressionPhase, isFirstMonth, isQuarterlyMonth, napCleanupComplete, full scoreResult including benchmark, full summaryHistory, AEO inventory, 4.3B identity, 12 narrative-eligible runs each truncated to 2000 chars) then section-by-section instructions that branch on Month 1 / Months 2-5 / Month 6+ in OPENING, isQuarterlyMonth (adds AI Authority Quarterly Report note), napCleanupComplete + Month 1 (NAP win priority), score === null (insufficient-data branch in WHERE WE STAND), trend UP/DOWN/FLAT/baseline, and Months 1-3 / 4-6 / 7+ in WHERE WE STAND. (2) narrative-engine.js — async generateNarrative(context). Direct call to https://api.anthropic.com/v1/messages (NOT through the Cloudflare Worker — the runner runs server-side in GitHub Actions). Headers: x-api-key + anthropic-version 2023-06-01. Body: model claude-sonnet-4-20250514, max_tokens 4000, temperature 0.7. 90s AbortController timeout. Returns null on any failure. (3) pdf-builder.js — async buildPDF(narrative, context). splitSections() parses Claude's text on the 8 literal headers using a line-anchored regex; paragraphize() escapes HTML and wraps blank-line-separated blocks in <p> with <br> for soft newlines; populateTemplate() does {{TOKEN}} replacement; Puppeteer (headless, --no-sandbox) renders Letter PDF with 1" margins, printBackground:true (for the WIN callout grey), page-number footer template, and writes to outputs/<client_id>_AI_Visibility_<YYYY-MM>.pdf. Returns null on any failure. (4) templates/report.html — self-contained HTML/CSS, no CDNs, no external images. Cover (Helvetica title, Georgia business name, italic author line), OPENING flows from the cover, every other section has a 14pt ALL-CAPS header + thin grey rule + page-break-before, WIN callout as an <aside> with grey background and dark left border, CLOSING ends with a centered footer line above a top border. Extended sheet-reader.js with readAEOInventory(clientConfig) — resolves columns BY NAME via the AEO_COLUMN_ALIASES map (accepts STATUS / Status / status, CLUSTER_NUMBER / Cluster_Number / Cluster Number, CLUSTER_NAME variants, STRATEGIC_OBJECTIVE / Strategic_Objective / Strategic Objective); returns totalLiveNodes + currentCluster (highest CLUSTER_NUMBER with at least one Live row, articleCount = ALL rows in the cluster regardless of status, strategicObjective null when absent) + nextCluster (lowest CLUSTER_NUMBER with no Live rows). Rewired runner.js: Step 1 also reads AEO_Inventory and fetches 4.3A + 4.3B via an inline googleapis Drive helper (Docs export to text/plain; plain files via files.get?alt=media; PLACEHOLDER and missing ids short-circuit to ""); computes progressionPhase (Foundation/Building/Authority by clientMonth tier), isFirstMonth, isQuarterlyMonth, napCleanupComplete; Step 6 assembles the full narrativeContext and calls generateNarrative; Step 7 calls buildPDF and logs the path + byte count. package.json bumped to 0.4.0, puppeteer ^23.0.0 added. Master Doc bumped to V1.2. Verified: every JS file passes node --check. Smoke tests pass with puppeteer + googleapis mocked: narrative-prompt user message contains all 24 required substrings including score, benchmark, AEO inventory, all 8 section names, and the correct Month 1 NAP-clean branch fires; pdf-builder splits all 8 sections out of a synthetic narrative, escapes XSS, replaces every {{TOKEN}}, and round-trips through buildPDF to a written file; runner exports include reportPeriodLabel (returns "May 2026" for 2026-05-14, "January 2026" for 2026-01-01, "December 2026" for 2026-12-31), progressionPhaseFor (1/3→Foundation, 4/6→Building, 7/24→Authority), and fetchDriveDocText short-circuits PLACEHOLDER / empty / null to "" without any network call; parser.js mock confirms x-api-key header sent with the configured key and stays on the LLM path; missing-key falls back to heuristic; narrative-engine mock confirms URL https://api.anthropic.com/v1/messages, model claude-sonnet-4-20250514, max_tokens 4000, temperature 0.7, x-api-key sent, and returns null when ANTHROPIC_API_KEY is unset; readAEOInventory mock confirms cluster 3 (highest live) becomes currentCluster with articleCount 3 and null strategicObjective, cluster 4 (lowest non-live) becomes nextCluster with its strategicObjective preserved, blank-cluster-number rows are dropped, and totalLiveNodes counts case-insensitive "live". Only drive-writer.js, index.html, the Cloudflare /trigger-report endpoint, and the .github/workflows/*.yml file remain — all targeted for Session 5+. |
| V1.6 | 2026-05-14 | Session 5 build log. Tool is FEATURE-COMPLETE in code. Built analysis-engine.js — single Claude Sonnet call to https://api.anthropic.com/v1/messages with model claude-sonnet-4-20250514, max_tokens 3000, temperature 0.3 (LOW — analysis not creative writing), 90s AbortController timeout. System prompt locks the role ("senior data analyst for iTech Valet"), the contract ("Your output must be a single valid JSON object. No preamble. No markdown fences."), and the honesty rule ("Do not manufacture wins. Do not hide problems."). User prompt assembles every fact the analyst needs (identity + month, score block with benchmark, full content inventory, NAP flag, all 13 run summaries one-line-each, all 13 raw responses each truncated to 2500 chars, full AI_Visibility_Summary history, citation history) and ends with the Strategic Brief JSON schema verbatim. Output recovery: stripCodeFences → JSON.parse → last-resort first{...last} slice; validateStrategicBrief enforces all 9 top-level keys (strategic_header, primary_insight, wins, recommended_wow_moment, concerns, pattern_analysis, competitor_intelligence, narrative_directives, forward_bridge). Any failure → null + log. Two-call architecture locked: analysis runs FIRST and sees ALL 13 runs (including Run 6); narrative runs SECOND, reads the brief, and sees the 12 client-eligible runs (Run 6 stripped before narrativeContext is built). Extended sheet-reader.js with readCitationHistory(clientConfig) — reads full AI_Visibility_Raw tab via googleapis (same service-account auth), resolves columns BY NAME via RAW_COLUMN_ALIASES (cited_urls, report_period, platform), splits cited_urls on commas/semicolons + http(s)://-filters + dedupes within the row, groups per URL with months Set + platforms Set + total count, keeps only URLs whose months Set size >= 2 (single-month is noise, multi-month is the locked-in signal), emits months as human labels ("April 2026") via periodToLabel, sorts by times_cited desc then url asc, returns [] on any failure. Built drive-writer.js — async uploadPDF(pdfPath, clientConfig) via googleapis Drive v3 with the same GOOGLE_SERVICE_ACCOUNT_JSON pattern as the Sheet modules; scope drive.file. PLACEHOLDER or missing drive_reports_folder_id short-circuits to null + a clear "skipping upload" log. Otherwise files.list with q "name = '<n>' and '<folder>' in parents and trashed = false" (escapes \\ and ' via escapeQueryString), then files.update if found (returns updated:true) or files.create if not (returns updated:false). supportsAllDrives set on every call. Any thrown error returns null + logs. Built index.html (replaces the Session 1 shell) — self-contained HTML/CSS, no external resources except system fonts, two operator-config values at the top of the inline script (INTERNAL_ACCESS_KEY placeholder "REPLACE_WITH_YOUR_KEY" + CLIENT_LIST array). Click handler POSTs JSON {client_id, internal_key} to https://anthropic-proxy.gerek.workers.dev/trigger-report AND sends the key as the x-internal-key header so the Worker can use either path. 2xx → green "Report triggered" + button stays disabled; non-2xx → red error with HTTP status + 140-char body excerpt; placeholder pre-flight check prints a setup error if INTERNAL_ACCESS_KEY hasn't been replaced. Built .github/workflows/run-report.yml — workflow_dispatch (with client_id input) + repository_dispatch (event_type "run-report" fired by the Worker). Single job ubuntu-latest, 30-min timeout, contents:write for the archive commit. Steps: checkout → setup-node 20 → npm install in AI-Visibility-Reporting → apt-get install Puppeteer/Chromium system deps (libnss3, libgbm-dev, libgtk-3-0, libasound2, libatk-bridge2.0-0, libatk1.0-0, libcups2, libdbus-1-3, libdrm2, libgbm1, libnspr4, libx11-xcb1, libxcomposite1, libxdamage1, libxfixes3, libxrandr2, libxss1, libxtst6, xdg-utils) → node runner.js --client=$CLIENT_ID with all 6 secrets injected as env (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, PERPLEXITY_API_KEY, GROK_API_KEY, GOOGLE_SERVICE_ACCOUNT_JSON) → git add archive/ + commit + push when there are new files. Updated prompts/narrative-prompt.js to consume analysisResult and repeatedCitations from the context: added a REPEATED CITATIONS block (one bullet per URL cited in 2+ months, or a "(none yet)" placeholder) and a STRATEGIC BRIEF FROM ANALYSIS ENGINE block (full JSON pretty-printed PLUS a "How to use this brief" cheat sheet; when analysisResult is null, replaces the brief with a clear "Analysis engine unavailable this run" instruction directing the writer to the original Month 1 / Month 2+ win priority order). A WIN WORTH NOTING instructions rewritten so the primary source is recommended_wow_moment.callout_text (rewrite in voice, keep every data point); when wins[] is empty Claude writes the exact "This month's numbers are building the foundation. Next month we'll have more to show." sentence; original priority order survives as the explicit fallback. Rewired runner.js to the 13-step architecture: Step 1 client config; Step 2 fetch 4.3A + 4.3B from Drive; Step 3 readSheetHistory + readAEOInventory + readCitationHistory (the citation reader is the new Session 5 call); Steps 4-5 13 platform runs (serial) + per-run parse; Step 6 score; Step 7 sheet write; Step 8 archive; Step 9 analysis-engine.generateAnalysis(analysisContext) — analysisContext.runs is the FULL 13-row array; Step 10 narrative-engine.generateNarrative(narrativeContext) — narrativeContext.runs is the 12-row filtered array (Run 6 stripped) + carries analysisResult; Step 11 buildPDF; Step 12 drive-writer.uploadPDF(pdfPath, clientConfig) — no more typeof guard, real call; Step 13 completion log line (client, period label, score, PDF path, Drive link, elapsed seconds). package.json bumped to 0.5.0; @anthropic-ai/sdk ^0.40.0 added per Session 5 spec (declared but not yet imported — parser/narrative/analysis all use raw fetch). Master Doc bumped to V1.3 with a new MANUAL SETUP — CLOUDFLARE WORKER /trigger-report ENDPOINT section documenting the route the operator needs to add to the existing Worker (POST /trigger-report, validates x-internal-key, fires GitHub repository_dispatch with event_type=run-report; needs INTERNAL_ACCESS_KEY + GITHUB_PAT env vars on the Worker). Verified: every JS file passes node --check. Smoke tests pass with puppeteer + googleapis + @anthropic-ai/sdk all mocked: analysis-engine sends correct URL/model/max_tokens 3000/temperature 0.3/x-api-key, the user prompt contains all 12 required substrings (identity, month, phase, score block, benchmark, AEO inventory, NAP flag, citation history, schema), invalid JSON returns null, missing top-level keys returns null, missing ANTHROPIC_API_KEY returns null; sheet-reader periodToLabel correctly maps 2026-05 → "May 2026" and falls through on garbage input; splitCitedUrls filters non-http strings and dedupes; readCitationHistory correctly groups two URLs across multiple months and filters out the single-month URL; drive-writer skips on PLACEHOLDER folder ID, skips on null folder ID, creates new file (returns updated:false) when files.list returns empty, updates existing file (returns updated:true) when files.list returns a match, escapeQueryString handles apostrophes + backslashes; narrative-prompt user prompt with brief contains the Strategic Brief JSON + the "Primary source" instruction + the exact "do NOT manufacture a win" sentence; fallback path (null brief) prints the documented "Analysis engine unavailable" instruction; runner.js loads cleanly with all Session 5 imports. Phase 1 is now feature-complete in code. Remaining work is operational: Cloudflare Worker /trigger-report route (manual), 6 GitHub Secrets provisioning, INTERNAL_ACCESS_KEY in index.html, real Sheet/Drive/Doc IDs in clients/itechvalet.json, service-account sharing on the Sheet + Drive folder, first end-to-end test run. |

---

## END OF DOCUMENT
